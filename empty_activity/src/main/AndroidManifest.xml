<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.os.operando.emptyactivity">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".EmailGlobal"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:label="@string/DominosPizzaApp"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".CreateAccount"
            android:label="@string/title_activity_create_account"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait"/>
        <activity android:name=".OrderPizza"
            android:screenOrientation="portrait"/>
        <activity android:name=".preCheckout"
            android:screenOrientation="portrait"/>
        <activity android:name=".Coupons"
            android:screenOrientation="portrait"/>
        <activity android:name=".RecentOrders"
            android:screenOrientation="portrait"/>
        <activity android:name=".SignIn2"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".ToppingChoice"
            android:label="ToppingChoice"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Location"
            android:screenOrientation="portrait"/>
        <activity android:name=".StoreList"
            android:screenOrientation="portrait"/>
        <activity android:name=".StoreDetails"
            android:screenOrientation="portrait"/>
        <activity android:name=".DeliveryMaps"
            android:screenOrientation="portrait"/>
        <activity android:name=".Tracking"
            android:screenOrientation="portrait"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".GoogleMaps"
            android:screenOrientation="portrait"
            android:label="@string/title_activity_google_maps" />
        <activity android:name=".MainPage2"
            android:screenOrientation="portrait"></activity>
    </application>

</manifest>